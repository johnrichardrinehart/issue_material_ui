import ReactDOM from "react-dom"; // Needed to attach to #root`
import React from "react"; // needed for rendering <App />, which is sugar for React.createElement
import Box from '@material-ui/core/Box';

ReactDOM.render(<Box />, document.getElementById("root"));
